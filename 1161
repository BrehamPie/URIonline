#include<bits/stdc++.h>
using namespace std;
unsigned long long fact(int n)
{
    if(n==0) return 1;
    return(n*fact(n-1));
}
int main()
{
    int n,m;
    while(cin>>n>>m && n!=EOF)
        cout<<fact(n)+fact(m)<<endl;
}
