#include<stdio.h>
#include<math.h>
int main()
{
    int i,cnt=0;float p,sum=0;
    for(i=0;i<6;i++)
    {
        scanf("%f",&p);
        if(p>0){cnt++;sum+=p;}

    }
    sum/=cnt;
    printf("%d valores positivos\n%.1f\n",cnt,sum);
    return 0;
}
