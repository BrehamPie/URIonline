#include<stdio.h>
#include<math.h>
int main()
{
    long long int in,max=-1,maxp=-1;
    for(int i=1;i<=100;i++)
    {
        scanf("%lld",&in);
        if(in>max)
        {
            max=in;
            maxp=i;
        }
    }
    printf("%lld\n%lld\n",max,maxp);
    return 0;
}
